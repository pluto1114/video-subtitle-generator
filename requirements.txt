# Core dependencies
numpy>=1.26.0,<2.0.0

# PyTorch with CUDA support (install from PyTorch index)
# Use: pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
torch>=2.5.0
torchaudio>=2.5.0

# ASR dependencies
ctranslate2>=4.0.0
faster-whisper>=1.0.0

# Audio processing
av>=10.0.0

# Utilities
tqdm>=4.65.0
pyyaml>=6.0
